# Task 8 - For Loops

## What does this do?
Read the code and guess what will happen. Then run it on the micro:bit and check your answer.

```python
from microbit import *

for i in range(3):
    display.show(str(i))
    sleep(500)
    display.clear()
    sleep(200)
```

## Modify the Loop
Change the code to count down from 3 to 1.