# Task 2 - Variables

## Fixing Code

!!! abstract "Instructions"
    The code below doesn't quite work. Add a variable to line 5 to make it work.

??? code "click to expand"

    ```python
    from microbit import *
    
    # Your code below
    name = Bob
    
    print(name)
    print(age)
    ```

??? tip "Hint - Click to expand"
    The name of the variable has to match something on line 7.

## Variable Names

!!! abstract "Instructions"
    The code below doesn't work. Edit the code to make it work.

    Complete the code below. Read the code and use the variable names to determine what to do:

??? code "click to expand"
    ```python
    from microbit import *

    num1 = 9
    num2 = 5
    
    print("The sum is: " + sum)
    print("The difference is: " + difference)
    print("The product is: " + product)
    ```

??? hint "Hint - Click to expand"
    *sum* is the total of num1 and num2.  It may be useful to create a variable to store the calculation.

## Display name and age

!!! abstract "Instructions"
    Using variables to set the `time`, `name`, `surname` and `age`. Display the name and surname, then wait for the time specified, and then display the age.
